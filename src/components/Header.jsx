import React from 'react';
import CartIcon from './CartIcon';
import Nav from './Nav';

const Header = () => {
  return (
    <header>
      <Nav />
      <CartIcon />
    </header>
  );
};

export default Header;
